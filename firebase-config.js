// firebase-config.js
// Placeholder module kept for compatibility with legacy HTML pages that reference it.
// This project currently uses an Express/Mongo backend so Firebase init is not required.
// If you plan to re-enable Firebase later, replace this file with your firebase configuration.

export default {};
